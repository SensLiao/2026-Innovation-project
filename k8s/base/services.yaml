apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: soma-ai
spec:
  selector:
    app: backend
  ports:
    - port: 3000
      targetPort: 3000
---
apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: soma-ai
spec:
  selector:
    app: frontend
  ports:
    - port: 80
      targetPort: 80
